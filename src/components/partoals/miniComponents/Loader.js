import React from 'react';
import LoaderImg from '../../../assets/img/loader_infinty.svg';

const Loader = () => {
  return (
    <div className='loader'>
    <img src={LoaderImg} alt={'hometex Loader'} />
    </div>
  );
};

export default Loader
